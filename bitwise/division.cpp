#include<bist/stdc++.h>
using namespace std;

int main()
{

    int did,div;
    cout<<"Enter the divident nd divisor: ";
    cin>>did>>div;

    if(div == did)
    {
        cout<<"The answer is 1 "<<endl;
        return 0;
    }

    int n = abs(did);
    int d = abs(div);

    while(n >= d)
    {
        
    }
    
}